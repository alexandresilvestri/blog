pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: "*.rb"
      run: bundle exec rubocop --autocorrect --force-exclusion {staged_files}
      stage_fixed: true
    rspec:
      run: bundle exec rspec
